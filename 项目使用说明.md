# 学历证书认证系统（ACVS）项目使用说明

> 基于 FISCO-BCOS 区块链的学历证书认证系统 —— 操作手册
> 配套文档：《开发环境搭建说明.md》（环境部署请先参阅该文档）
> 本文档面向系统的三类使用者（院校 / 毕业生 / 企业），描述各角色的功能与操作步骤。

---

## 一、系统简介

本系统用于解决学历证书的 **防伪、查验、吊销** 问题。院校将证书的 PDF 指纹（SHA-256 哈希）写入区块链，任何人都无法篡改；企业验证时将待查证书的指纹与链上记录比对，从而判断证书真伪。

系统设有三种角色，登录时需明确选择：

| 角色 | 标识 | 核心职责 |
| --- | --- | --- |
| **院校** | `COLLEGE` | 录入学生证书、生成证书 PDF、将证书指纹上链、吊销证书 |
| **毕业生** | `STUDENT` | 查看 / 下载本人的学历证书 |
| **企业** | `COMPANY` | 查询学生信息、验证证书真伪（数据库 + 区块链双重验证） |

**访问入口**：启动前后端后，浏览器打开 `http://localhost:5173`，自动跳转到登录页。

---

## 二、通用操作：注册、登录与完善信息

### 2.1 注册账号

1. 登录页点击 **【注册】** 进入注册页。
2. 填写信息并提交：
   - **用户名**：5~16 位非空字符，全系统唯一
   - **密码**：5~16 位非空字符（后端以 MD5 加密存储）
   - **用户类型**：院校 / 毕业生 / 企业（三选一）
3. 提示「注册成功」后返回登录页。

### 2.2 登录

1. 在登录页输入 **用户名、密码**，并选择对应的 **用户类型**（三者必须与注册时一致，否则提示「用户名或密码错误」）。
2. 登录成功后系统签发 JWT 令牌（有效期 **6 小时**，缓存在 Redis），并按角色自动跳转：
   - 院校 → `/college`
   - 毕业生 → `/student`
   - 企业 → `/company`

> 💡 令牌过期或退出登录后，需要重新登录。

### 2.3 首次登录必须完善基本信息 ⭐

**首次登录会被强制跳转到「基本信息」页**，必须先完善以下信息才能使用其他功能：

- **真实姓名 / 名称**（`realName`）：学生填真实姓名；院校填院校相关名称；企业填企业名称
- **身份证号**（`idCard`）：18 位，需通过格式校验，且全系统唯一

> ⚠️ **院校账号特别注意**：院校录入证书时，证书上的「毕业院校」字段会自动取自院校账户真实姓名字段中 **以 `&` 分隔的第二段**（即 `realName.split('&')[1]`）。因此院校在完善信息时，真实姓名建议按 `法人姓名&院校名称` 的格式填写（例如 `张三&XX大学`），否则证书 PDF 上的院校名称可能显示异常。

### 2.4 修改密码 / 退出登录

各角色菜单的「账户」分组下均提供 **修改密码** 与 **退出登录**。退出会清除本地令牌并返回登录页。

---

## 三、院校（COLLEGE）操作指南

院校是证书的颁发方，左侧菜单分为 **学生管理、链操作、账户** 三组。颁发一张证书需依次完成下面 **4 个步骤**。

### 📌 颁发证书完整流程（核心）

```
① PDF生成  →  ② PDF上传(OSS)  →  ③ 生成Hash指纹  →  ④ 确认信息并上链
```

#### 步骤 ① PDF 生成（菜单：学生管理 → PDF生成）

1. 填写学历证明信息：
   - **学生姓名**、**专业名称**
   - **毕业院校**：自动带入（来自院校账户信息，只读）
   - **学位类型**：专科 / 本科 / 硕士 / 博士
   - **毕业时间**：日期选择器
   - 可选上传一张图片（JPG/PNG，≤2MB）
2. 点击 **【生成学历证明】**，前端使用 `html2canvas + jsPDF` 将证明渲染为 A4 PDF 并 **下载到本地**。

#### 步骤 ② PDF 上传（菜单：学生管理 → PDF上传）

将上一步生成的 PDF 上传到 **阿里云 OSS**，系统返回该文件的访问 URL（对应后端 `POST /upload` 接口）。此 URL 将作为证书的 `pdf_path` 存档。

#### 步骤 ③ 生成 Hash（菜单：链操作 → 生成Hash）

对证书 PDF 计算 **SHA-256 指纹**（前端使用 `crypto-js`）。该指纹（`fileHash`）是证书的唯一防伪标识，将被写入区块链。

#### 步骤 ④ 确认信息并上链（菜单：链操作 → 确认信息并上传）

确认证书信息无误后提交上链（对应后端 `POST /certificate/issue`）。后端会：

1. **自动生成 16 位证书编号**，规则为：`C + 当天日期(yyyyMMdd) + 7位随机串`，例如 `C20260602a1b2c3d`。
2. 调用智能合约 `CertHashRegistry.issue(certNo, fileHash, collegeId)` 将指纹写入区块链。
3. 交易成功后，把证书信息（含 `cert_no`、`file_hash`、`tx_hash` 交易哈希、`status=1`）存入数据库。
4. 返回「证书颁发成功」。

> ✅ 颁发成功的标志：数据库 `certificate` 表新增记录且 `tx_hash` 字段有值。

### 📌 撤销证书（菜单：链操作 → 撤销证书）

输入要撤销的 **证书编号**（`certNo`）提交（对应 `POST /certificate/revoke`）。后端调用合约 `revoke(certNo)`，链上状态置为「已撤销」，数据库中该证书 `status` 同步改为 `0`。撤销后该证书在企业端验证时将返回「失效」。

---

## 四、毕业生（STUDENT）操作指南

毕业生菜单较为简洁，主要用于查看本人证书。

| 菜单 | 功能 |
| --- | --- |
| 基本信息 | 查看 / 完善个人信息（真实姓名、身份证号） |
| 获取证书 | 输入本人姓名查询学历证书，获取 PDF 的 OSS 链接，可在线查看或下载 |
| 修改密码 | 修改登录密码 |
| 退出登录 | 退出当前账号 |

**获取证书操作**：在「获取证书」页输入学生姓名查询（对应 `GET /certificate/get-certificate`），系统返回该生证书 PDF 的访问地址。若该生尚未被颁发证书，提示「该学生未颁发证书」。

---

## 五、企业（COMPANY）操作指南

企业是证书的查验方，可对持有的证书进行真伪验证。

| 菜单 | 功能 |
| --- | --- |
| 基本信息 | 查看 / 完善企业信息 |
| 查询学生 | 查询学生 / 证书相关信息 |
| 验证证书 | 验证证书真伪（数据库验证 + 区块链验证） |
| 修改密码 / 退出登录 | 账户管理 |

### 验证证书（核心）

系统提供 **两种验证方式**，验证时需提供证书的关键信息（证书编号 `certNo`、文件指纹 `fileHash`、颁发院校 `collegeId`）：

**方式一：数据库验证**（对应 `POST /verifyLog/getVerifyLogSql`）
- 根据证书指纹 `fileHash` 在数据库中查找记录。
- 命中且 `status=1`（正常）→ 返回 **PASS**；查不到或已撤销 → 返回 **FAIL**。
- 速度快，适合初步查验。

**方式二：区块链验证**（对应 `POST /verifyLog/getVerifyLog`）
- 先在数据库校验证书存在且未撤销。
- 再调用智能合约 `verify(certNo, fileHash, collegeId)` 与 **链上指纹比对**。
- 三项全部匹配且证书状态正常 → 返回 **PASS**，否则 **FAIL**。
- 防篡改能力最强，是最权威的验证方式。

> 📝 **验证留痕**：无论验证通过与否，每次验证都会在 `verify_log` 表写入一条记录（含验证企业、证书、结果 PASS/FAIL、验证时间），便于审计追溯。

---

## 六、典型业务场景演示（答辩可用）

一个完整的「颁发—验证—吊销」闭环演示流程：

1. **注册 3 个账号**：分别为院校、毕业生、企业。
2. **院校登录** → 完善信息（真实姓名填 `法人&XX大学`）→ 依次完成 PDF生成 → PDF上传 → 生成Hash → 上链，颁发一张证书。
3. **查看链上数据**：在 WeBASE / 数据库确认 `tx_hash` 已生成，证明已上链。
4. **企业登录** → 用该证书信息做 **区块链验证** → 返回 **PASS**。
5. **院校登录** → 撤销该证书。
6. **企业再次验证** → 返回 **FAIL**（证明吊销生效、链上可信）。
7. （可选）**篡改演示**：故意修改 `fileHash` 后验证 → 返回 **FAIL**，证明防篡改能力。

---

## 七、后端接口清单（附录）

> 接口文档（Swagger）：后端启动后访问 `http://localhost:8081/swagger-ui.html`

| 模块 | 方法 | 路径 | 说明 |
| --- | --- | --- | --- |
| 用户 | POST | `/user/register` | 注册（用户名/密码/角色） |
| 用户 | POST | `/user/login` | 登录，返回 JWT 令牌 |
| 用户 | GET | `/user/userInfo` | 查询当前登录用户信息 |
| 用户 | PUT | `/user/update` | 完善 / 更新基本信息（真实姓名、身份证） |
| 用户 | PUT | `/user/update-password` | 修改密码 |
| 用户 | PATCH | `/user/updatePDF` | 更新证书 PDF 地址 |
| 文件 | POST | `/upload` | 上传文件到阿里云 OSS，返回 URL |
| 证书 | POST | `/certificate/issue` | 颁发证书并上链 |
| 证书 | GET | `/certificate/get-certificate` | 按学生姓名查询证书 PDF 地址 |
| 证书 | POST | `/certificate/revoke` | 撤销证书 |
| 验证 | POST | `/verifyLog/getVerifyLogSql` | 数据库验证证书 |
| 验证 | POST | `/verifyLog/getVerifyLog` | 区块链验证证书 |

---

## 八、常见使用问题（FAQ）

**Q1：登录提示「用户名或密码错误」，但账号确实存在？**
A：检查 **用户类型** 是否选对。系统按「用户名 + 密码 + 角色」三者联合校验，角色选错也会登录失败。

**Q2：登录后操作接口提示未授权 / 跳回登录页？**
A：JWT 令牌有效期为 6 小时，过期需重新登录；也可能是 Redis 未启动导致令牌校验失败。

**Q3：证书上链失败 / 颁发失败？**
A：① 确认 FISCO-BCOS 节点正常运行、`config.toml` 的节点 IP 正确；② 确认证书编号为 16 位（系统自动生成，正常不会出错）；③ 同一证书编号不可重复颁发（合约会报 `certNo exists`）。

**Q4：企业验证一直返回 FAIL？**
A：核对 `certNo`、`fileHash`、`collegeId` 三项是否与颁发时完全一致；任一不符或证书已被撤销都会返回 FAIL。

**Q5：院校生成的证书 PDF 上「毕业院校」显示不对？**
A：院校账户的「真实姓名」需按 `法人姓名&院校名称` 格式填写，系统取 `&` 后面的部分作为院校名（见 2.3）。

**Q6：证书 PDF 打不开 / 下载不了？**
A：PDF 存储在阿里云 OSS，请确认 OSS 配置正确、Bucket 可访问，且 `application.yml` 中的 AccessKey 有效。

---

*文档生成日期：2026-06-02*
