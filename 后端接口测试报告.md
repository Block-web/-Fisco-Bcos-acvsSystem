# 学历证书认证系统（ACVS）后端接口测试报告

> 测试对象：基于 FISCO-BCOS 区块链的学历证书认证系统后端服务（`acvsystem`）
> 测试日期：2026-06-02
> 测试方式：对运行中的后端服务（`http://localhost:8081`）进行真实接口调用（黑盒）+ 源码走查（白盒）
> 测试范围：全部 4 个控制器、共 **11 个接口**，累计 **38 个测试用例**

---

## 一、测试概述

本次测试对 ACVS 后端的全部对外 REST 接口进行了功能验证、参数校验验证、鉴权验证、异常路径验证，并完整跑通了「颁发证书 → 验证 → 撤销 → 再验证」的核心业务闭环。

测试采用**安全实测**策略：

- 对会破坏现有真实数据的接口（`update-password` 全表改密码、`issue` 上链、`revoke` 撤销、`upload` 上传 OSS）采取保护措施；
- 改密码接口测试前用 SQL 备份、测试后立即恢复；
- 上链 / 撤销仅使用**新建的测试证书**，绝不触碰现有 4 条真实证书；
- 测试结束后清理全部临时数据，环境已还原至测试前基线。

### 测试结果总评

| 维度 | 结论 |
| --- | --- |
| 接口连通性 | ✅ 11/11 接口均可正常路由与响应 |
| 鉴权机制 | ✅ JWT + Redis 双重校验，未授权请求正确返回 401 |
| 正常业务功能 | ✅ 注册/登录/查询/更新/颁发/验证/撤销主流程全部走通 |
| 区块链交互 | ✅ 上链（issue）、链上验证（verify）、撤销（revoke）真实成功 |
| 参数与异常健壮性 | ⚠️ 存在 **1 个严重缺陷 + 多处空指针（NPE）+ 异常信息暴露** 等问题 |

**核心结论：主流程功能完整可用，但健壮性与安全性存在需要修复的缺陷，其中 `update-password` 全表越权改密码为严重缺陷，建议优先修复。**

---

## 二、测试环境

| 组件 | 状态 | 说明 |
| --- | --- | --- |
| 后端服务 | ✅ 运行中 | Spring Boot，端口 `8081`，由 JDK 21 启动 |
| MySQL | ✅ 8.0.43 | 数据库 `acvsystem`，3 张表 |
| Redis | ✅ 运行中 | 端口 `6379`，存储 JWT 令牌（有效期 6 小时） |
| FISCO-BCOS | ✅ 节点可达 | 节点 `192.168.1.104:20200`，合约地址 `0xc8ead4b26b2c6ac14c9fd90d9684c9bc2cc40085` |
| 阿里云 OSS | ⚠️ 上传失败 | 广州区，Bucket `acvs`；实测上传未成功（详见缺陷 D3） |

统一响应格式：`Result{ code, message, data }`，`code=0` 成功、`code=1` 失败。

测试基线数据：`user` 3 条、`certificate` 4 条、`verify_log` 42 条。

---

## 三、接口测试结果总览

| # | 模块 | 方法 | 路径 | 鉴权 | 用例 | 结果 | 备注 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 1 | 用户 | POST | `/user/register` | 否 | 7 | ✅ 通过 | 参数校验、重复名、非法角色均正确拦截 |
| 2 | 用户 | POST | `/user/login` | 否 | 6 | ✅ 通过 | 三因子（名/密/角色）校验，返回 JWT |
| 3 | 用户 | GET | `/user/userInfo` | 是 | 3 | ✅ 通过 | 密码字段正确隐藏；无效令牌 401 |
| 4 | 用户 | PUT | `/user/update` | 是 | 4 | ✅ 通过 | 姓名/身份证空、格式、重复均拦截 |
| 5 | 用户 | PUT | `/user/update-password` | 是 | 2 | ⚠️ 缺陷 | **D1：全表越权改密码（严重）** |
| 6 | 文件 | POST | `/upload` | 是 | 3 | ⚠️ 缺陷 | **D3：OSS 上传失败被掩盖为成功** |
| 7 | 证书 | POST | `/certificate/issue` | 是 | 2 | ✅ 通过 | 真实上链成功，生成 tx_hash |
| 8 | 证书 | GET | `/certificate/get-certificate` | 是 | 3 | ⚠️ 缺陷 | 正常路径 OK；**D2：查无记录 NPE** |
| 9 | 证书 | POST | `/certificate/revoke` | 是 | 2 | ⚠️ 注意 | 撤销成功；**D5：幂等语义不严谨** |
| 10 | 验证 | POST | `/verifyLog/getVerifyLogSql` | 是 | 3 | ⚠️ 缺陷 | PASS/FAIL OK；**D2：查无记录 NPE** |
| 11 | 验证 | POST | `/verifyLog/getVerifyLog` | 是 | 3 | ⚠️ 缺陷 | 链上 PASS/FAIL OK；**D2：查无记录 NPE** |

> 说明：`/user/page` 在源码中为注释代码、`/user/updatePDF` 仅在旧文档出现而源码中不存在，二者均非有效接口，未纳入测试。

---

## 四、详细测试用例与实测结果

> 以下「响应」均为接口真实返回原文。

### 4.1 用户模块

#### 接口 1：`POST /user/register` 注册

| 用例 | 输入 | 期望 | 实际响应 | 判定 |
| --- | --- | --- | --- | --- |
| R1 | col / test12345 / COLLEGE | 成功 | `{code:0, data:"用户:apitest_col注册成功"}` | ✅ |
| R2 | stu / test12345 / STUDENT | 成功 | `{code:0, data:"用户:apitest_stu注册成功"}` | ✅ |
| R3 | com / test12345 / COMPANY | 成功 | `{code:0, data:"用户:apitest_com注册成功"}` | ✅ |
| R4 | 重复用户名 apitest_col | 失败 | `{code:1, message:"用户名已存在"}` | ✅ |
| R5 | 用户名 `abc`（<5 位） | 校验失败 | `{code:1, message:"register.username: 需要匹配正则表达式\"^\\S{5,16}$\""}` | ✅ |
| R6 | 角色 `ADMIN`（非法） | 校验失败 | `{code:1, message:"register.role: 角色参数的值只能是 COLLEGE、STUDENT 或 COMPANY"}` | ✅ |
| R7 | 密码 `123`（<5 位） | 校验失败 | `{code:1, message:"register.password: 需要匹配正则表达式\"^\\S{5,16}$\""}` | ✅ |

#### 接口 2：`POST /user/login` 登录

| 用例 | 输入 | 期望 | 实际响应 | 判定 |
| --- | --- | --- | --- | --- |
| L1 | col 正确凭证 | 返回 token | `{code:0, data:"eyJhbGci...（JWT）"}` | ✅ |
| L1b/L1c | stu / com 正确凭证 | 返回 token | 均返回 JWT（载荷含 id/role/username） | ✅ |
| L2 | 密码错误 | 失败 | `{code:1, message:"用户名或密码错误"}` | ✅ |
| L3 | 角色不匹配（col 选 STUDENT） | 失败 | `{code:1, message:"用户名或密码错误"}` | ✅ |
| L4 | 用户不存在 | 失败 | `{code:1, message:"用户名或密码错误"}` | ✅ |

> JWT 载荷示例（Base64 解码）：`{"claims":{"role":"COLLEGE","id":4,"username":"apitest_col"},"exp":1780408394}`。登录成功后令牌写入 Redis，有效期 6 小时。

#### 接口 3：`GET /user/userInfo` 查询当前用户信息

| 用例 | 输入 | 期望 | 实际响应 | 判定 |
| --- | --- | --- | --- | --- |
| U1 | 带有效 token | 返回用户信息，**不含密码** | `{code:0, data:{id:4,username:"apitest_col",role:"COLLEGE",realName:null,idCard:null,...}}` | ✅ 密码字段被 `@JsonIgnore` 隐藏 |
| U2 | 不带 token | 401 | HTTP 401，空响应体 | ✅ |
| U3 | 非法 token | 401 | HTTP 401，空响应体 | ✅ |

#### 接口 4：`PUT /user/update` 更新基本信息

| 用例 | 输入 | 期望 | 实际响应 | 判定 |
| --- | --- | --- | --- | --- |
| UP1 | username + realName(含中文) + 合法 idCard | 成功 | `{code:0, data:"用户信息更新成功"}` | ✅ 中文 `测试法人&测试大学` 正确落库 |
| UP2 | 缺 realName | 失败 | `{code:1, message:"真实姓名或身份证号不能为空"}` | ✅ |
| UP3 | idCard=`123`（格式错误） | 失败 | `{code:1, message:"身份证号格式错误"}` | ✅ |
| UP4 | idCard 使用已存在的号码 | 失败 | `{code:1, message:"身份证号已存在"}` | ✅ |

#### 接口 5：`PUT /user/update-password` 修改密码 ⚠️

| 用例 | 输入 | 期望 | 实际响应 | 判定 |
| --- | --- | --- | --- | --- |
| PW1 | username=apitest_col, password=newpass99 | 仅改该用户 | `{code:0, data:"密码更新成功"}`，但**全表 6 个用户密码全部被改** | ❌ 见缺陷 D1 |
| PW2 | username=不存在用户 | 失败 | `{code:1, message:"用户未登录"}` | ⚠️ 文案不准确（应为"用户不存在"） |

**PW1 缺陷证据**（数据库快照）：

```
测试前：  id=1~3 → 4297f44b...(123456)   id=4~6 → c06db68e...(test12345)
仅调用一次 update-password(username=apitest_col)
测试后：  id=1~6 全部 → 7fd3d24a...(newpass99 的 MD5)   ← 6 个用户密码被同时篡改
```

> 测试后已通过备份 SQL 立即恢复 id=1/2/3 的密码。

---

### 4.2 文件模块

#### 接口 6：`POST /upload` 上传文件至阿里云 OSS ⚠️

| 用例 | 输入 | 期望 | 实际响应 | 判定 |
| --- | --- | --- | --- | --- |
| F1 | 带 token，上传文本文件 | 返回 OSS URL | `{code:0, data:""}` ← **url 为空字符串** | ❌ 见缺陷 D3 |
| F2 | 不带 token | 401 | HTTP 401 | ✅ |
| F3 | 带 token，不携带文件 | 友好报错 | `{code:1, message:"Cannot invoke \"...MultipartFile.getOriginalFilename()\" because \"file\" is null"}` | ⚠️ NPE 暴露（D4） |

---

### 4.3 证书模块

#### 接口 7：`POST /certificate/issue` 颁发证书（上链）

| 用例 | 输入 | 期望 | 实际响应 | 判定 |
| --- | --- | --- | --- | --- |
| C1 | 完整测试证书 | 上链成功 | `{code:0, data:"证书颁发成功"}`，耗时 4.4s | ✅ 真实上链 |
| C2 | 缺 studentName | 校验失败 | `{code:1, message:"Validation failed ... field 'studentName': rejected value [null] ... 不能为null"}` | ✅（信息暴露，见 D4） |

> C1 成功后数据库新增证书：`cert_no=C20260602d5a4002`，`college_id=4`，`status=1`，`tx_hash=0x2c51e461...`（真实交易哈希），证明指纹已写入区块链。

#### 接口 8：`GET /certificate/get-certificate` 按学生姓名查询证书

| 用例 | 输入 | 期望 | 实际响应 | 判定 |
| --- | --- | --- | --- | --- |
| G1b | studentName=测试学生apitest（显式 UTF-8 编码） | 返回 pdfPath | `{code:0, data:"https://acvs.oss-cn-guangzhou.aliyuncs.com/apitest_cert.pdf"}` | ✅ 正常路径通过 |
| G2 | 不存在的学生 | 应提示"未颁发证书" | `{code:1, message:"Cannot invoke \"...Certificate.getPdfPath()\" because \"certificate\" is null"}` | ❌ NPE，见缺陷 D2 |
| G1 | 中文姓名经 `--data-urlencode` 传参 | 返回 pdfPath | 返回 NPE（查不到） | ⚠️ 中文 query 编码问题，见 D6 |

#### 接口 9：`POST /certificate/revoke` 撤销证书（上链）

| 用例 | 输入 | 期望 | 实际响应 | 判定 |
| --- | --- | --- | --- | --- |
| RV1 | 撤销测试证书 C20260602d5a4002 | 成功 | `{code:0, data:"撤销成功：Success"}`，DB `status` 同步置 0 | ✅ 真实上链撤销 |
| RV2 | 撤销不存在的证书号 | 应报错 | `{code:0, data:"撤销成功：already revoked"}` | ⚠️ 仍返回成功，见缺陷 D5 |

---

### 4.4 验证模块

#### 接口 10：`POST /verifyLog/getVerifyLogSql` 数据库验证

| 用例 | 输入 | 期望 | 实际响应 | 判定 |
| --- | --- | --- | --- | --- |
| SQL1 | 正常证书三要素（status=1） | PASS | `{code:0, data:"验证结果：PASS"}` | ✅ |
| SQL3 | 已撤销证书（status=0） | FAIL | `{code:1, message:"该证书不存在与数据库或该证书已失效"}` | ✅ 优雅 FAIL |
| SQL2 | 证书不存在 | FAIL | `{code:1, message:"Cannot invoke \"...Certificate.getId()\" because \"selectCertNo\" is null"}` | ❌ NPE，见缺陷 D2 |

#### 接口 11：`POST /verifyLog/getVerifyLog` 区块链验证

| 用例 | 输入 | 期望 | 实际响应 | 判定 |
| --- | --- | --- | --- | --- |
| BC1 | 正常证书三要素（status=1） | 链上 PASS | `{code:0, data:{id:44,certId:5,companyId:6,result:"PASS",...}}` | ✅ 真实链上验证 |
| BC3 | 已撤销证书（status=0） | FAIL | `{code:1, message:"该证书不存在与数据库或该证书已失效"}` | ✅ 优雅 FAIL |
| BC2 | fileHash 篡改（查不到） | FAIL | `{code:1, message:"Cannot invoke \"...Certificate.getId()\" because \"CertNo\" is null"}` | ❌ NPE，见缺陷 D2 |

> 注：返回的 `verifyLog` 对象中 `verifyType` 与 `verifyTime` 为 `null`（代码未赋值，依赖数据库默认值），`verify_type` 字段（QUERY/BLOCKCHAIN）的业务区分实际未实现。

---

## 五、缺陷清单

| 编号 | 严重程度 | 接口 | 缺陷概述 |
| --- | --- | --- | --- |
| D1 | 🔴 严重 | update-password | 全表越权改密码 |
| D2 | 🟠 高 | get-certificate / getVerifyLogSql / getVerifyLog | 查无记录时空指针（NPE） |
| D3 | 🟡 中 | upload | OSS 上传失败被掩盖为成功 |
| D4 | 🟡 中 | 全局 | 原始异常/校验信息直接返回前端 |
| D5 | 🔵 低 | revoke | 撤销不存在/已撤销证书仍返回成功 |
| D6 | 🔵 低 | get-certificate | 中文 query 参数编码隐患 |

### D1【严重】update-password 全表越权改密码

- **位置**：`UserController.java:140-149`
- **根因**：
  ```java
  // 缺少 .eq(User::getUsername, username) 更新条件
  userService.lambdaUpdate().set(User::getPassword, Md5Util.getMD5String(password)).update();
  ```
  `lambdaUpdate().set(...).update()` 没有任何 `where` 条件，MyBatis-Plus 会执行 `UPDATE user SET password=?`（全表更新）。
- **复现**：用例 PW1，仅指定 `apitest_col`，结果 6 个用户密码被同时改写（已实测证据）。
- **影响**：① 任意一个用户改密码会重置**所有人**的密码，直接导致全系统登录瘫痪；② 接口未校验「调用者是否有权修改该 username」，任意登录用户可越权。属于严重的数据与权限缺陷。
- **建议**：
  ```java
  userService.lambdaUpdate()
      .set(User::getPassword, Md5Util.getMD5String(password))
      .eq(User::getUsername, username)   // 必须补充更新条件
      .update();
  ```
  并增加权限校验：只允许修改当前登录用户（从 ThreadLocal 取 username），或限定管理员。

### D2【高】查无记录时空指针（NPE）

涉及 3 处，模式相同：**先调用对象方法、后做 null 判断**，导致记录不存在时直接 NPE 崩溃（被全局异常兜底为 `code=1`，但信息为 Java 报错而非业务提示）。

- `CertificateController.java:104` — `certificate.getPdfPath()` 在 `certificate` 可能为 null 时调用；第 105 行才判断 `pdfPath == null`（且判断对象错误）。
- `VerifyLogController.java:113` — `verifyLog.setCertId(selectCertNo.getId())` 在第 116 行的 `selectCertNo == null` 判断**之前**执行。
- `VerifyLogController.java:61-65` — 进入 `if (CertNo == null || ...)` 分支后，第 65 行仍调用 `CertNo.getId()`。
- **影响**：企业验证一张不存在/被篡改的证书时本应记录一条 FAIL 日志并友好提示，实际却抛 NPE，FAIL 审计日志也未能写入。
- **建议**：调整为先判空、再使用；查无记录时返回明确业务提示（如「该学生未颁发证书」「证书不存在」）并补记 FAIL 日志。

### D3【中】OSS 上传失败被掩盖为成功

- **位置**：`AliOssUtil.java:62-78`
- **根因**：`catch (OSSException) / catch (ClientException)` 仅 `System.out.println` 打印，**未重新抛出**，`url` 变量保持初始值 `""`，方法照常 `return ""`。
- **现象**：用例 F1 上传后接口返回 `code:0, data:""`——OSS 实际上传失败（凭证失效/网络/权限等），但前端会误判为成功并拿到空 URL。
- **建议**：捕获异常后应向上抛出或返回失败标识，由 Controller 返回 `Result.error(...)`；同时校验 `PutObjectResult` 与 url 非空。

### D4【中】原始异常/校验信息直接返回前端

- **位置**：`GlobalExceptionHandler.java:21` 直接返回 `e.getMessage()`。
- **现象**：参数校验失败返回正则表达式原文、`Validation failed for argument...` 全限定类名、NPE 的内部方法签名等（见 R5/R6/C2/各 NPE 用例）。
- **影响**：暴露内部实现细节（类名、字段、技术栈），既不友好也有信息泄露风险。
- **建议**：按异常类型分类处理，对外返回脱敏的友好中文提示，详细堆栈仅记录到服务端日志。

### D5【低】撤销语义不严谨

- **位置**：`CertificateController.java:113-142`
- **现象**：撤销不存在的证书号返回 `{code:0, data:"撤销成功：already revoked"}`（用例 RV2）。接口未先校验证书是否存在于数据库，且无论合约返回什么都以 `Result.success` 包装。
- **建议**：撤销前先查库校验证书存在且状态正常；依据合约 `returnMessage` 判断真实结果，失败时返回 `Result.error`。

### D6【低】中文 query 参数编码隐患

- **现象**：`get-certificate` 以中文姓名作为 `@RequestParam` 查询，当客户端编码与服务端解码不一致时查不到记录（用例 G1 失败、G1b 显式 UTF-8 编码成功）。
- **建议**：统一前后端为 UTF-8（确认 Tomcat `URIEncoding=UTF-8`，Spring Boot 默认已是）；或改为 POST + 请求体传参，规避 URL 编码问题。

---

## 六、业务闭环验证（核心场景）

完整跑通了《项目使用说明》第六节描述的「颁发—验证—吊销」闭环：

| 步骤 | 操作 | 结果 |
| --- | --- | --- |
| ① 颁发 | issue 新证书 `C20260602d5a4002` | ✅ 上链成功，生成 tx_hash |
| ② 数据库验证 | getVerifyLogSql（status=1） | ✅ PASS |
| ③ 区块链验证 | getVerifyLog（三要素匹配） | ✅ 链上 PASS |
| ④ 撤销 | revoke 该证书 | ✅ 链上撤销，DB status=0 |
| ⑤ 撤销后再验证 | getVerifyLogSql / getVerifyLog | ✅ 均返回 FAIL（已失效） |

闭环逻辑正确，区块链「防伪—查验—吊销」能力得到真实验证。

---

## 七、安全性观察（附加）

| 项 | 观察 | 建议 |
| --- | --- | --- |
| 凭证硬编码 | `AliOssUtil.java:20-21` 将 OSS AccessKey/Secret 明文写死在源码 | 改为环境变量/配置中心，并轮换密钥 |
| 配置明文 | `application.yml` 中数据库密码、OSS 凭证明文 | 使用配置加密或密钥管理 |
| 密码哈希 | 用户密码采用 MD5（无盐） | 升级为 BCrypt/Argon2 加盐哈希 |
| 越权控制 | update / update-password 未校验「操作者 = 数据归属者」 | 引入基于 JWT 身份的资源归属校验 |
| 合约部署 | issue/revoke/verify 每次都 `deployByContractLoader` 重新部署合约 | 应只加载已部署合约地址，避免重复部署开销 |

---

## 八、测试数据与环境还原说明

本次测试遵循「测试后清理」原则，已完全还原环境：

- ✅ 删除测试用户 `apitest_col/stu/com`
- ✅ 删除测试证书 `C20260602d5a4002`
- ✅ 删除本次新增的 `verify_log` 记录（id>42）
- ✅ 恢复被 D1 缺陷连带修改的现有用户（id=1/2/3）密码
- ✅ 清理 Redis 中的测试令牌

还原后数据量已回到基线：**user=3、certificate=4、verify_log=42**。

> 注：`upload` 测试可能在 OSS 留有一个临时小文件（实测上传失败，预计未产生对象）；区块链上 issue/revoke 的测试交易不可逆，已永久留存于链上（这是区块链特性，符合预期，不影响业务数据）。

---

## 九、改进建议汇总（按优先级）

1. **【P0】** 修复 `update-password` 全表更新缺陷（补 `where` 条件 + 权限校验）。
2. **【P1】** 统一修复 3 处查无记录 NPE，改为「先判空、后使用」并返回友好业务提示。
3. **【P1】** 修复 `upload` 异常吞噬，上传失败应明确返回错误。
4. **【P2】** 全局异常处理脱敏，避免返回原始堆栈/类名/正则。
5. **【P2】** 将硬编码的 OSS 凭证移出源码；密码哈希升级加盐。
6. **【P3】** 完善 revoke 前置校验与结果判定；落实 `verify_type` 的 QUERY/BLOCKCHAIN 区分。

---

## 附录：测试脚本与原始记录

本次测试的脚本与逐用例原始响应保存于项目下 `api-test-workspace/` 目录：

| 文件 | 内容 |
| --- | --- |
| `test_user_A.sh` / `result_user_A.txt` | 注册 + 登录 |
| `test_user_B.sh` / `result_user_B.txt` | userInfo + update + update-password（含 D1 证据） |
| `test_upload.sh` / `result_upload.txt` | 文件上传 |
| `test_cert_issue.sh` / `result_cert_issue.txt` | 证书颁发上链 |
| `test_cert_verify.sh` / `result_cert_verify.txt` | 证书查询 + 验证 |
| `test_revoke.sh` / `result_revoke.txt` | 撤销 + 撤销后验证闭环 |
| `restore_password.sql` | 密码恢复备份 |

> 上述脚本可重复执行用于回归测试（注意：会再次产生测试数据，建议测试后按本报告第八节方式清理）。

---

*报告生成日期：2026-06-02*
