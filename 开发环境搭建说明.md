# 学历证书认证系统（ACVS）开发环境搭建说明

> 基于 FISCO-BCOS 区块链的学历证书认证系统（Academic Credential Verification System）
> 本文档面向毕业设计答辩与二次开发，描述从零搭建本地开发环境的完整步骤。

---

## 一、系统架构总览

本系统采用 **前后端分离 + 联盟链** 架构，由四大部分组成：

| 模块 | 目录 | 技术栈 | 说明 |
| --- | --- | --- | --- |
| 前端 | `acvs/` | Vue 3 + Vite 7 + Element Plus + Pinia + Vue Router + Axios | 院校 / 学生 / 企业三种角色的 Web 界面 |
| 后端 | `acvsystem/` | Spring Boot 2.7.0 + Java 14 + MyBatis-Plus + JWT | REST API 服务，端口 `8081` |
| 数据库 | `asvsystem.sql` | MySQL 8.x + Redis | 业务数据持久化 + 登录令牌缓存 |
| 区块链 | `CertHashRegistry.sol` | FISCO-BCOS 3.x + WeBASE 3.7.0 | 存储证书哈希指纹，防篡改 |
| 文件存储 | — | 阿里云 OSS | 存放证书 PDF 文件 |

### 数据流简述

1. **院校** 录入证书 → 生成 PDF 上传 OSS → 计算 SHA-256 指纹 → 调用智能合约 `issue()` 上链 → 交易哈希存入 MySQL。
2. **学生** 登录后查看 / 下载本人证书。
3. **企业** 输入证书编号验证 → 后端比对链上哈希（`verify()`）→ 记录验证日志。
4. **院校** 可调用 `revoke()` 吊销证书。

---

## 二、环境要求清单

搭建前请准备以下软件（括号内为本项目实际使用 / 推荐版本）：

| 软件 | 版本要求 | 用途 | 备注 |
| --- | --- | --- | --- |
| **JDK** | **14**（必须） | 后端编译运行 | `pom.xml` 锁定 `java.version=14`、编译目标 14 |
| **Maven** | 3.6+ | 后端依赖管理 | 可用 IDEA 内置 Maven |
| **Node.js** | `^20.19.0` 或 `>=22.12.0` | 前端构建运行 | `package.json` 的 `engines` 已限定 |
| **npm** | 随 Node.js 安装 | 前端包管理 | 也可用 pnpm/yarn |
| **MySQL** | 8.x | 业务数据库 | 库名 `acvsystem` |
| **Redis** | 3.2+（项目自带 Windows 版） | 登录令牌缓存 | 见 `Redis-x64-3.2.100/` |
| **FISCO-BCOS** | 3.x | 联盟链节点 | 建议部署在 WSL2 / Linux |
| **WeBASE** | 3.7.0 | 区块链管理 + 合约部署 | 内置 FISCO-BCOS Java SDK 3.6.0 |
| **IDE** | IntelliJ IDEA / VS Code | 开发 | 后端用 IDEA，前端用 VS Code |
| **Git** | 任意较新版本 | 版本控制 | — |

> ⚠️ **JDK 版本注意**：本项目强制使用 **Java 14**，使用 Java 8 / 11 / 17 会因编译目标不匹配而失败。FISCO-BCOS SDK 也对 JDK 版本较敏感，建议使用 JDK 14（OpenJDK 14 即可）。

---

## 三、搭建步骤

> 建议按以下顺序搭建：**基础环境 → MySQL → Redis → FISCO-BCOS + 合约部署 → 阿里云 OSS → 后端 → 前端**。

### 3.1 安装基础环境

1. 安装 **JDK 14**，配置 `JAVA_HOME` 环境变量，验证：
   ```bash
   java -version    # 应显示 14.x
   ```
2. 安装 **Maven**（或直接用 IDEA 自带），验证：
   ```bash
   mvn -version
   ```
3. 安装 **Node.js 20.19+ / 22.12+**，验证：
   ```bash
   node -v
   npm -v
   ```

---

### 3.2 部署 MySQL 数据库

后端默认连接配置（见 `acvsystem/src/main/resources/application.yml`）：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/acvsystem?useSSL=false&serverTimezone=UTC
    username: root
    password: 123456
```

**操作步骤：**

1. 安装并启动 MySQL 8.x（端口 `3306`）。
2. 创建数据库：
   ```sql
   CREATE DATABASE acvsystem DEFAULT CHARSET utf8mb4;
   ```
3. 导入表结构脚本 `asvsystem.sql`（位于项目根目录），它会创建三张核心表：
   - `user`：三角色统一登录表（COLLEGE / STUDENT / COMPANY），密码 MD5 加密
   - `certificate`：学历证书表（含 `file_hash` PDF 指纹、`tx_hash` 链上交易哈希、`status` 状态）
   - `verify_log`：企业验证日志表
   ```bash
   mysql -uroot -p123456 acvsystem < asvsystem.sql
   ```
4. 如果你的 MySQL 用户名 / 密码不同，请同步修改 `application.yml` 中的 `username` / `password`。

> 💡 表之间存在外键约束（`certificate.college_id → user.id` 等），导入时若报错请确认 MySQL 引擎为 InnoDB。

---

### 3.3 部署 Redis

项目已自带 Windows 版 Redis（`Redis-x64-3.2.100/`），后端默认连接：

```yaml
spring:
  redis:
    host: localhost
    port: 6379
```

**启动方式（Windows）：**

```bash
# 进入目录后双击 start.bat，或命令行执行：
cd Redis-x64-3.2.100
redis-server.exe redis.windows.conf
```

启动后另开窗口验证：

```bash
redis-cli.exe ping    # 返回 PONG 即正常
```

> Redis 用于缓存 JWT 登录令牌。若不启动，登录相关接口会报连接异常。
> macOS / Linux 用户可用 `brew install redis` 或包管理器自行安装，端口保持 6379 即可。

---

### 3.4 部署 FISCO-BCOS 区块链与智能合约 ⭐（核心）

这是本系统区别于普通 CRUD 项目的关键。推荐使用 **WeBASE 一键部署工具**（内置 FISCO-BCOS 3.x 节点 + 可视化合约管理）。

#### 3.4.1 部署区块链节点

1. 在 **WSL2（Ubuntu）或 Linux 服务器** 上安装 FISCO-BCOS 3.x（建议搭配 WeBASE 3.7.0 一键部署）。
   > 项目使用 FISCO-BCOS Java SDK `3.6.0`（见 `pom.xml`），对应链版本为 3.x。
2. 启动后默认会拉起 4 个节点，监听端口 `20200 ~ 20203`。

#### 3.4.2 部署智能合约

1. 合约源码位于项目根目录 `CertHashRegistry.sol`（Solidity `^0.8.0`），核心方法：
   - `issue(certNo, fileHash, collegeId)`：发证上链
   - `verify(certNo, fileHash, collegeId)`：验证证书真伪
   - `revoke(certNo)`：吊销证书
2. 通过 WeBASE 控制台或 `console` 部署该合约到 `group0` 群组。
3. **部署后会得到一个合约地址**，例如本项目当前使用：
   ```
   0xc8ead4b26b2c6ac14c9fd90d9684c9bc2cc40085
   ```

#### 3.4.3 配置后端连接区块链

**① 修改节点连接配置** `acvsystem/src/main/resources/conf/config.toml`：

```toml
[network]
defaultGroup = "group0"
peers=["172.23.119.2:20200", "172.23.119.2:20201", "172.23.119.2:20202", "172.23.119.2:20203"]
```

> ⚠️ **重点**：`172.23.119.2` 是 WSL2 的内网 IP，**WSL 每次重启可能变化**。请用 `ip addr`（Linux 内）或 `wsl hostname -I` 查询当前 IP，并同步修改此处 `peers` 列表。

**② 替换 SDK 证书**：将链上生成的 SDK 证书（`ca.crt`、`sdk.crt`、`sdk.key`）拷贝到 `acvsystem/src/main/resources/conf/` 目录，覆盖现有文件。这些证书由 WeBASE / 节点生成，用于 SSL 双向认证。

**③ 替换合约地址**：合约地址当前 **硬编码** 在两个 Controller 中，部署新合约后必须同步修改：

```java
// acvsystem/src/main/java/com/zheng/acvsystem/controller/CertificateController.java:49
// acvsystem/src/main/java/com/zheng/acvsystem/controller/VerifyLogController.java:49
public static final String CONTRACTADDRESS = "0x你的新合约地址";
```

**④ ABI / BIN 文件**：合约的 ABI 和 BIN 已放在 `acvsystem/src/main/resources/abi/CertHashRegistry.abi` 和 `bin/CertHashRegistry.bin`。若重新编译了合约，需用新的 ABI/BIN 覆盖。

---

### 3.5 配置阿里云 OSS（证书 PDF 存储）

后端使用阿里云 OSS 存放生成的证书 PDF，配置见 `application.yml`：

```yaml
aliyun:
  oss:
    endpoint: https://oss-cn-guangzhou.aliyuncs.com
    access-key-id: 你的AccessKeyId
    access-key-secret: 你的AccessKeySecret
    bucket-name: acvs
    region: cn-guangzhou
```

**操作步骤：**

1. 注册阿里云账号，开通 OSS 服务，创建一个 Bucket（如 `acvs`，公共读或按需）。
2. 在阿里云 RAM 控制台创建 AccessKey，获取 `AccessKeyId` / `AccessKeySecret`。
3. 将上述配置替换为自己的值，`endpoint` / `region` 改为你 Bucket 所在地域。

> 🔒 **安全提醒**：仓库 `application.yml` 中现有的 AccessKey 已随代码公开，存在泄露风险。**请务必在阿里云控制台禁用 / 删除该 Key，并替换为你自己的新 Key**，切勿直接使用。

---

### 3.6 启动后端 acvsystem

1. 用 **IntelliJ IDEA** 打开 `acvsystem/` 目录（识别为 Maven 项目）。
2. 确认 IDEA 的 Project SDK 设为 **JDK 14**。
3. 等待 Maven 下载依赖（首次较慢）。若 FISCO-BCOS SDK 依赖下载失败，可能需要在 Maven `settings.xml` 中添加 FISCO 官方仓库：
   ```xml
   <repository>
     <id>fisco</id>
     <url>https://oss.sonatype.org/content/repositories/snapshots/</url>
   </repository>
   ```
4. 确保 **MySQL、Redis、FISCO-BCOS 节点** 均已启动。
5. 运行启动类 `com.zheng.acvsystem.AcvsystemApplication`，或命令行：
   ```bash
   cd acvsystem
   mvn spring-boot:run
   ```
6. 启动成功后后端运行在 **`http://localhost:8081`**。
7. 接口文档（Swagger，已在 `application.yml` 中 `swagger.enabled: true` 开启）：
   ```
   http://localhost:8081/swagger-ui.html
   ```

---

### 3.7 启动前端 acvs

1. 进入前端目录安装依赖：
   ```bash
   cd acvs
   npm install
   ```
2. 启动开发服务器：
   ```bash
   npm run dev
   ```
3. 默认运行在 **`http://localhost:5173`**（Vite 7 默认端口）。
4. 前端已配置代理（见 `vite.config.js`），所有 `/api` 开头的请求会转发到后端 `8081` 并去掉 `/api` 前缀：
   ```js
   server: {
     proxy: {
       '/api': {
         target: 'http://localhost:8081',
         changeOrigin: true,
         rewrite: path => path.replace(/^\/api/, '')
       }
     }
   }
   ```
5. 生产构建：
   ```bash
   npm run build      # 产物输出到 dist/
   npm run preview    # 本地预览构建产物
   ```

---

## 四、启动顺序与联调验证

为保证服务依赖正常，**请严格按以下顺序启动**：

```
① MySQL  →  ② Redis  →  ③ FISCO-BCOS 节点  →  ④ 后端 acvsystem  →  ⑤ 前端 acvs
```

**验证清单：**

- [ ] `redis-cli ping` 返回 `PONG`
- [ ] MySQL 中 `acvsystem` 库存在三张表
- [ ] FISCO-BCOS 节点端口 `20200-20203` 可连通（`telnet IP 20200`）
- [ ] 后端启动日志无异常，可访问 `http://localhost:8081/swagger-ui.html`
- [ ] 前端 `http://localhost:5173` 可正常打开登录页
- [ ] 用院校账号录入一张证书，能在 MySQL `certificate` 表看到 `tx_hash`，说明上链成功
- [ ] 用企业账号验证该证书返回「通过」，`verify_log` 表新增一条记录

---

## 五、端口与配置速查表

| 服务 | 默认端口 | 配置文件位置 |
| --- | --- | --- |
| 后端 Spring Boot | `8081` | `acvsystem/src/main/resources/application.yml` |
| 前端 Vite | `5173` | `acvs/vite.config.js` |
| MySQL | `3306` | `application.yml`（库名 `acvsystem`，root/123456） |
| Redis | `6379` | `application.yml` |
| FISCO-BCOS 节点 | `20200~20203` | `acvsystem/src/main/resources/conf/config.toml` |
| 合约地址 | — | `CertificateController.java:49`、`VerifyLogController.java:49` |
| 阿里云 OSS | — | `application.yml` 的 `aliyun.oss` |

---

## 六、常见问题（FAQ）

**Q1：后端启动报 FISCO-BCOS 连接超时 / SSL 握手失败？**
A：① 检查 `config.toml` 的 `peers` IP 是否为当前 WSL2 实际 IP；② 确认 `conf/` 下的 `ca.crt`、`sdk.crt`、`sdk.key` 是当前链生成的最新证书；③ 确认节点已启动且端口可达。

**Q2：调用 `issue` 报「certNo length error」？**
A：合约要求证书编号 `certNo` 长度必须为 **16 位**（见 `CertHashRegistry.sol`），请检查编号生成规则。

**Q3：Maven 找不到 `fisco-bcos-java-sdk` 3.6.0？**
A：在 Maven `settings.xml` 增加 FISCO 仓库地址，或从 WeBASE 安装目录手动安装到本地仓库。

**Q4：前端请求 404 / 跨域？**
A：确认后端已在 `8081` 启动，且通过前端 `/api` 前缀访问（代理已处理跨域）。直接 `fetch('http://localhost:8081/...')` 会有跨域问题。

**Q5：JDK 版本不对导致编译失败？**
A：`pom.xml` 锁定 Java 14，请在 IDEA 的 `File → Project Structure → Project SDK` 中选择 JDK 14。

**Q6：登录后接口报 Redis 连接异常？**
A：JWT 令牌缓存依赖 Redis，请先启动 Redis（`redis-server.exe redis.windows.conf`）。

---

## 七、安全注意事项

> 以下为公开仓库中的敏感信息，二次开发时**务必处理**：

1. **阿里云 OSS AccessKey** 已明文写在 `application.yml` 并提交到仓库 —— 请立即在阿里云控制台**作废原 Key 并更换**。
2. **数据库密码** `123456` 为弱口令，生产环境请修改。
3. **合约地址、SDK 证书** 属于环境相关配置，建议改用环境变量 / 配置中心管理，避免硬编码。
4. 建议将 `application.yml`、`conf/*.key` 等敏感文件加入 `.gitignore`，改用 `application-example.yml` 模板提交。

---

*文档生成日期：2026-06-02*
